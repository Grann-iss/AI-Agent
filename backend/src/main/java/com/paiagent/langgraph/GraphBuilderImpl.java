package com.paiagent.langgraph;

import com.langgraph4j.GraphBuilder;
import com.paiagent.entity.Workflow;

public class GraphBuilderImpl {
    public void build(Workflow workflow) {
        // 使用 LangGraph4j 构建图
        GraphBuilder builder = new GraphBuilder();
        // ...
    }
}
